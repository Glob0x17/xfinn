# Architecture des Sous-titres - Diagrammes

## 📊 Vue d'ensemble du système

```
┌─────────────────────────────────────────────────────────────────┐
│                        Application xfinn                         │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    MediaDetailView                        │  │
│  │                                                           │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐  │  │
│  │  │  UI Button  │→│ State Manager │→│ Auto-Selection │  │  │
│  │  └─────────────┘  └──────────────┘  └────────────────┘  │  │
│  │         │                │                    │          │  │
│  └─────────┼────────────────┼────────────────────┼──────────┘  │
│            │                │                    │              │
│            ▼                ▼                    ▼              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                 UserDefaults (Preferences)                │  │
│  │              preferredSubtitleLanguage: String?           │  │
│  └──────────────────────────────────────────────────────────┘  │
│            │                                                     │
│            ▼                                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   JellyfinService                         │  │
│  │                                                           │  │
│  │  getSubtitleURL() → URL pour télécharger les sous-titres │  │
│  └──────────────────────────────────────────────────────────┘  │
│            │                                                     │
│            ▼                                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                      AVPlayer                             │  │
│  │                                                           │  │
│  │  AVPlayerItem → AVMediaSelectionGroup → Sous-titres      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flux de données - Sélection manuelle

```
┌──────────┐
│ Utilisat.│
└────┬─────┘
     │ Clique sur "Sous-titres"
     ▼
┌─────────────────┐
│ showSubtitlePic │
│ ker = true      │
└────┬────────────┘
     │ Affiche Alert
     ▼
┌─────────────────────────────┐
│  Liste des pistes           │
│  - Aucun                    │
│  - Français (fra)           │
│  - English (eng)            │ ◄─── item.subtitleStreams
│  - Español (spa)            │
└────┬────────────────────────┘
     │ Sélectionne "Français"
     ▼
┌──────────────────────────────────────┐
│ selectedSubtitleIndex = 1            │
│ preferredSubtitleLanguage = "fra"    │
└────┬─────────────────────────────────┘
     │ Sauvegarde
     ▼
┌──────────────────────────────────────┐
│ UserDefaults.standard.set(           │
│   "fra",                             │
│   forKey: "preferredSubtitleLanguage"│
│ )                                    │
└────┬─────────────────────────────────┘
     │ Met à jour l'UI
     ▼
┌──────────────────────────────────────┐
│ Bouton affiche:                      │
│ [●] Français                         │
│     (icône remplie + couleur accent) │
└──────────────────────────────────────┘
```

---

## 🎬 Flux de données - Lecture vidéo

```
┌─────────────────┐
│ startPlayback() │
└────┬────────────┘
     │
     ▼
┌──────────────────────────────────────┐
│ continueStartPlayback()              │
│                                      │
│ 1. Créer AVURLAsset(streamURL)      │
│ 2. Créer AVPlayerItem(asset)        │
└────┬─────────────────────────────────┘
     │
     │ Si selectedSubtitleIndex != nil
     ▼
┌──────────────────────────────────────┐
│ addExternalSubtitles()               │
│                                      │
│ • Obtenir l'URL du sous-titre        │
│   via JellyfinService                │
│ • Créer AVURLAsset pour les ST       │
│ • Charger les tracks                 │
└────┬─────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────┐
│ Observer status du PlayerItem        │
│                                      │
│ for await status in                  │
│   playerItem.publisher(...).values   │
└────┬─────────────────────────────────┘
     │
     │ Quand status == .readyToPlay
     ▼
┌──────────────────────────────────────┐
│ enableSubtitlesInPlayer()            │
│                                      │
│ 1. Obtenir AVMediaSelectionGroup     │
│ 2. Trouver l'option correspondante   │
│ 3. playerItem.select(option, in:...) │
└────┬─────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────┐
│ Lecture avec sous-titres activés ✅  │
└──────────────────────────────────────┘
```

---

## 🔍 Flux de données - Auto-sélection

```
┌──────────────┐
│ View.onAppear│
└────┬─────────┘
     │
     ▼
┌────────────────────────────────────────┐
│ Charger preferredSubtitleLanguage      │
│ depuis UserDefaults                    │
└────┬───────────────────────────────────┘
     │
     ▼
┌────────────────────────────────────────┐
│ autoSelectSubtitles()                  │
└────┬───────────────────────────────────┘
     │
     ├─── Langue préférée existe?
     │
     │ OUI ────────────────────────────────┐
     │                                     │
     ▼                                     ▼
┌──────────────────────────┐   ┌──────────────────────────┐
│ Chercher dans            │   │ Chercher sous-titre      │
│ item.subtitleStreams     │   │ par défaut               │
│ où language == preferred │   │ (isDefault == true)      │
└────┬─────────────────────┘   └────┬─────────────────────┘
     │                               │
     │ Trouvé?                       │ Trouvé?
     │                               │
     │ OUI          NON ◄────────────┴─── OUI
     │               │                     │
     ▼               ▼                     ▼
┌────────────────────────────────────────────┐
│ selectedSubtitleIndex = subtitle.index     │
│ ✅ Sous-titres pré-sélectionnés            │
└────────────────────────────────────────────┘
```

---

## 🏗️ Architecture des composants

```
┌─────────────────────────────────────────────────────┐
│                  MediaDetailView                    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  States:                                            │
│  • selectedSubtitleIndex: Int?                      │
│  • preferredSubtitleLanguage: String?               │
│  • showSubtitlePicker: Bool                         │
│                                                     │
│  Computed Properties:                               │
│  • selectedSubtitleDisplayName: String              │
│                                                     │
│  Functions:                                         │
│  • autoSelectSubtitles()                            │
│  • addExternalSubtitles(to:subtitle:)               │
│  • enableSubtitlesInPlayer(playerItem:)             │
│                                                     │
└──────────────┬──────────────────────────────────────┘
               │
               │ utilise
               │
               ▼
┌──────────────────────────────────────────────────────┐
│                  JellyfinService                     │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Methods:                                            │
│  • getSubtitleURL(                                   │
│      itemId: String,                                 │
│      mediaSourceId: String,                          │
│      streamIndex: Int,                               │
│      format: String                                  │
│    ) -> URL?                                         │
│                                                      │
│  Returns:                                            │
│  https://server/Videos/{id}/{id}/Subtitles/{idx}/   │
│  Stream.vtt?api_key=...                              │
│                                                      │
└──────────────┬───────────────────────────────────────┘
               │
               │ communique avec
               │
               ▼
┌──────────────────────────────────────────────────────┐
│                  Jellyfin Server                     │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Endpoints:                                          │
│  • GET /Videos/{id}/{mediaSourceId}/                 │
│        Subtitles/{index}/Stream.{format}             │
│                                                      │
│  Retourne:                                           │
│  • Fichier WebVTT avec les sous-titres              │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

## 📦 Structure des données

```
MediaItem
├── id: String
├── name: String
├── type: String
├── ...
└── mediaStreams: [MediaStream]?
    └── filter { type == "Subtitle" }
        │
        └── MediaStream
            ├── index: Int                    (identifiant unique)
            ├── type: String                  ("Subtitle")
            ├── displayTitle: String?         ("Français", "English")
            ├── language: String?             ("fra", "eng")
            ├── codec: String?                ("webvtt", "srt")
            ├── isDefault: Bool?              (true/false)
            ├── isForced: Bool?               (true/false)
            └── deliveryUrl: String?          (URL si externe)
```

---

## 🎨 États de l'interface

```
┌─────────────────────────────────────────────────────┐
│             Bouton Sous-titres - États              │
└─────────────────────────────────────────────────────┘

État 1: Aucun sous-titre
┌──────────────────────────┐
│ [○] Aucun                │  ← Icône vide
│                          │    Couleur: Secondaire
└──────────────────────────┘    Fond: Glass standard


État 2: Sous-titre sélectionné
┌──────────────────────────┐
│ [●] Français             │  ← Icône remplie
│                          │    Couleur: Primaire (Accent)
└──────────────────────────┘    Fond: Primaire avec opacité
                                Bordure: Primaire


État 3: Pendant sélection
┌──────────────────────────┐
│  Sous-titres             │  ← Alert iOS/tvOS
│                          │
│  Choisissez les sous-    │
│  titres à afficher...    │
│                          │
│  • Aucun                 │
│  • Français              │
│  • English               │
│  • Español               │
│                          │
│  [Annuler]               │
└──────────────────────────┘
```

---

## 🔄 Cycle de vie des sous-titres

```
1. Chargement de la vue
   onAppear()
      │
      ├─ Charger préférences UserDefaults
      └─ autoSelectSubtitles()

2. Sélection manuelle
   Bouton cliqué
      │
      ├─ Afficher Alert
      ├─ Utilisateur choisit
      ├─ Mettre à jour @State
      └─ Sauvegarder préférence

3. Démarrage lecture
   startPlayback()
      │
      ├─ Créer AVPlayer
      ├─ addExternalSubtitles()
      └─ enableSubtitlesInPlayer()

4. Pendant lecture
   AVPlayerItem.status = .readyToPlay
      │
      └─ Sous-titres activés automatiquement

5. Fin de lecture
   stopPlayback()
      │
      └─ Nettoyer ressources (player, observers)

6. Sortie de la vue
   onDisappear()
      │
      └─ État sauvegardé pour prochaine fois
```

---

## 🧩 Intégration AVFoundation

```
AVPlayer
   │
   └── AVPlayerItem
          │
          └── AVURLAsset (video)
                 │
                 ├── tracks (video/audio)
                 │
                 └── AVMediaSelectionGroup (.legible)
                        │
                        └── options: [AVMediaSelectionOption]
                               │
                               ├─ Option 1: Français
                               ├─ Option 2: English
                               └─ Option 3: Español
                                     │
                                     └── playerItem.select(option, in: group)
                                            │
                                            ▼
                                     Sous-titres affichés ✅
```

---

## 🔐 Persistance des données

```
UserDefaults.standard
   │
   └── "preferredSubtitleLanguage": String?
          │
          ├─── Sauvegarde
          │    └─ Quand utilisateur sélectionne une piste
          │       UserDefaults.standard.set(language, forKey: ...)
          │
          ├─── Lecture
          │    └─ Au chargement de MediaDetailView
          │       let lang = UserDefaults.standard.string(forKey: ...)
          │
          └─── Suppression
               └─ Quand utilisateur choisit "Aucun"
                  UserDefaults.standard.removeObject(forKey: ...)
```

---

## 📡 Communication réseau

```
Application                           Serveur Jellyfin
    │                                      │
    │ 1. GET /Users/{id}/Items/{itemId}   │
    │ ───────────────────────────────────>│
    │                                      │
    │ 2. Retourne MediaItem avec          │
    │    mediaStreams (sous-titres)       │
    │ <───────────────────────────────────│
    │                                      │
    │ 3. Utilisateur sélectionne piste    │
    │                                      │
    │ 4. GET /Videos/{id}/{id}/Subtitles/ │
    │    {index}/Stream.vtt?api_key=...   │
    │ ───────────────────────────────────>│
    │                                      │
    │ 5. Retourne fichier WebVTT          │
    │ <───────────────────────────────────│
    │                                      │
    │ 6. Charger dans AVPlayer            │
    │                                      │
    ▼                                      ▼
```

---

## ⚙️ Gestion des erreurs

```
addExternalSubtitles()
   │
   ├─ getSubtitleURL() retourne nil?
   │  └─ ❌ Log: "Impossible de créer l'URL"
   │     └─ Continuer sans sous-titres
   │
   ├─ Chargement des tracks échoue?
   │  └─ ❌ Log: "Erreur lors du chargement"
   │     └─ Fallback: Continuer sans sous-titres
   │
   └─ Succès
      └─ ✅ Log: "Piste chargée"

enableSubtitlesInPlayer()
   │
   ├─ legibleGroup == nil?
   │  └─ ⚠️ Log: "Aucun groupe disponible"
   │     └─ Continuer sans sous-titres
   │
   ├─ Aucune option correspondante?
   │  └─ ⚠️ Log: "Aucune piste trouvée"
   │     └─ Essayer première option disponible
   │
   └─ Succès
      └─ ✅ Log: "Sous-titres activés: {name}"
```

---

**Note :** Ces diagrammes sont en format texte ASCII pour faciliter la lecture dans un éditeur de code. Pour une présentation, ils peuvent être convertis en diagrammes graphiques avec des outils comme Mermaid, PlantUML ou Lucidchart.

---

**Dernière mise à jour :** 22 décembre 2024
